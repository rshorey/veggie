<%= form_for(@common_allergen) do |f| %>
  <% if @common_allergen.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@common_allergen.errors.count, "error") %> prohibited this common_allergen from being saved:</h2>

      <ul>
      <% @common_allergen.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.label :name %>:
  <%= f.text_field :name %><br />
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
